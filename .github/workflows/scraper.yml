name: Scrape Jobs

on:
  schedule:
    # Every 2 hours
    - cron: '0 */2 * * *'
  workflow_dispatch:
    inputs:
      source:
        description: 'Specific scraper source to run (leave blank for all)'
        required: false
        default: ''

jobs:
  scrape:
    name: Run Job Scrapers
    runs-on: ubuntu-latest
    timeout-minutes: 60

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install --no-cache-dir -r requirements.txt

      - name: Run scrapers
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          TYPESENSE_ENABLED: 'false'
          REDIS_URL: ''
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          APIFY_API_TOKEN: ${{ secrets.APIFY_API_TOKEN }}
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          PLAYWRIGHT_ENABLED: 'false'
        run: |
          if [ -n "${{ github.event.inputs.source }}" ]; then
            echo "Running scraper for source: ${{ github.event.inputs.source }}"
            python main.py scrape --source ${{ github.event.inputs.source }}
          else
            echo "Running all scrapers..."
            python main.py scrape
          fi

      - name: Report result
        if: always()
        run: echo "Scraper job finished with status ${{ job.status }}"
